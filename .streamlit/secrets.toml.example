# Streamlit Community Cloud: paste these into the app's Settings > Secrets (never commit real values).
# Locally this file is not needed: the dashboard runs in full-control mode.

# Password the group types on the web app to record or correct trades (share it only with the group)
EDIT_PASSWORD = "choose-a-password"

# GitHub fine-grained personal access token, so trades saved on the web app are committed to the repo:
# github.com > Settings > Developer settings > Fine-grained tokens > Generate new token;
# Repository access: only shambo1597-afk/capex-infra-portfolio; Permissions: Contents = Read and write
GITHUB_TOKEN = "github_pat_..."
